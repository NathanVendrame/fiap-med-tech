- Antes de iniciar, abra o docker desktop.
- Para rodar a aplicação basta rodar o seguinte comando:

```
docker compose up
```

Ao acessar os endpoints protegidos, não esqueça de incluir o JWT token gerado dos logins em seus requests.